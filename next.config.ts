import { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // The swcMinify option is no longer needed in Next.js 13+
  // as it's enabled by default
};

export default nextConfig;
